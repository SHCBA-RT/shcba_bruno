meta {
  name: firmMembers
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/admin/firms/:id/members
  body: none
  auth: bearer
}

params:query {
  ~limit: 3
  ~page: 2
  ~email: example
  ~id: 274
  ~sort: id:asc
  ~sort: createdAt:desc
}

params:path {
  id: 111
}

auth:bearer {
  token: {{SupeAdminToken}}
}

settings {
  encodeUrl: true
}
