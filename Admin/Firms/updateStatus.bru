meta {
  name: updateStatus
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/admin/firms/:id/update-status
  body: json
  auth: bearer
}

params:path {
  id: 43
}

auth:bearer {
  token: {{userToken}}
}

body:json {
  {
    "status": "active"
  //   "adminId": 2
  }
  
}

settings {
  encodeUrl: true
}
