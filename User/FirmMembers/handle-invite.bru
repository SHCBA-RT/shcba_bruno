meta {
  name: handle-invite
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/user/firm-member/handleInvite
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
      "action":"accepted",
      "inviteId":23
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
