meta {
  name: listFirms
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/firm/firms/list
  body: json
  auth: bearer
}

params:query {
  ~page: 2
  ~limit: 20
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
      "search": "l"
  }
}

settings {
  encodeUrl: true
}
