meta {
  name: forgotPassword
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/user/auth/forgot-password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
      "email": "bilal.khan@example.com"
  }
}

settings {
  encodeUrl: true
}
