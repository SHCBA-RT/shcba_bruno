meta {
  name: Login
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/user/auth/login
  body: json
  auth: inherit
}

body:json {
  {
      "email": "AliCo4@example.com",
      "password": "password123"
  }
}

script:post-response {
  const response = res.getBody();
  if(response.success){
      bru.setEnvVar('accessToken', response.data.accessToken)
  }
}

settings {
  encodeUrl: true
}
