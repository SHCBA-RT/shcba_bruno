meta {
  name: UpdateWorkshop
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/api/admin/workshops/:id
  body: json
  auth: bearer
}

params:path {
  id: 2
}

auth:bearer {
  token: {{SupeAdminToken}}
}

body:json {
  {
      "title": "Full Workshop Test",
      "isNotif": true
  }
}

settings {
  encodeUrl: true
}
